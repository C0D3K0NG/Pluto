# Pluto Voice Assistant - Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies (required)
SpeechRecognition>=3.10.0
PyAutoGUI>=0.9.54
PyGetWindow>=0.0.9

# Optional dependencies
keyboard>=0.13.5      # For hotkey support
playsound==1.2.2      # For custom sound files (use 1.2.2 for compatibility)
pyttsx3>=2.90         # For voice feedback
pystray>=0.19.4       # For system tray icon
pillow>=10.0.0        # For tray icon image

# Note: The following are built-in Windows libraries (no install needed):
# - winsound (Windows system beeps)
# - logging, threading, json, os, sys, time
